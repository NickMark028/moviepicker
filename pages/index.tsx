import React from "react";

import { Header, Footer, Homepage } from "@components";

const Home: React.FC = () => {
    return (
        <>
            <Header />
            <Homepage />
            <Footer />
        </>
    );
};

export default Home;
